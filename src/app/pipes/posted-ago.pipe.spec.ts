import { PostedAgoPipe } from './posted-ago.pipe';

describe('PostedAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new PostedAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
